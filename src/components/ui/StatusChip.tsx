// Same visual pattern as Badge.tsx, offered under the more conventional
// "StatusChip" name for status-specific usage (order/invoice/contract state)
// so call sites read clearly. Re-exports Badge rather than duplicating logic.
export { Badge as StatusChip } from "./Badge";
